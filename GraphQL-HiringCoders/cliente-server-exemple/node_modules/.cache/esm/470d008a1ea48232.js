let createServer;_1ea‍.w("http",[["createServer",["createServer"],function(v){createServer=v}]]);

const server = createServer((request, respose) => {
    switch(request.url){
        case '/status': {
            response.writeHead(200);
            response.write('Okay');
            response.end();
            return;
        }
    }
});

server.listen(8000, '127.0.0.1', () => {
    console.log("server is listenning at http://127.0.0.1:8000");
});